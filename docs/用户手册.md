# TanmiWorkspace 用户手册

> AI 编程任务的结构化管理方案

---

## 这是什么？

TanmiWorkspace 就像是 AI 的项目管理系统。

想象你在和一个能干但健忘的同事合作——他很聪明，但对话一长就忘了早期的决策；任务一复杂就容易跑偏。TanmiWorkspace 就是给这个"同事"配的任务管理工具：把大任务拆成小步骤，每步都记录下来，随时能回顾进度。

### 核心概念

| 概念 | 说明 |
|------|------|
| **工作台** | TanmiWorkspace 本身，管理所有工作区的平台 |
| **工作区** | 单个任务的完整记录，包含一棵任务树 |
| **节点** | 任务树中的每个任务单元 |

工作台是你的办公桌，工作区是桌上的每个项目文件夹，节点是文件夹里的待办事项。

> 更多概念（节点类型、状态、派发模式等）请参阅 [术语表](#术语表)。

### 与普通 AI 对话的区别

| 对比 | 普通 AI 对话 | 使用 TanmiWorkspace |
|------|-------------|-------------------|
| **流程控制** | 靠提示词「建议」，AI 可能忽略 | 系统强制约束，AI 必须遵守 |
| **任务记录** | 对话结束即丢失 | 完整持久化，随时恢复 |
| **执行追溯** | 不知道 AI 做了什么 | WebUI 实时展示，每步可查 |
| **复杂任务** | 容易跑偏、遗漏 | 树形拆分，逐个验收 |
| **跨会话** | 每次从头开始 | 接续上次进度继续 |

> 不是「希望」AI 按流程做，而是「强制」AI 必须按流程做。

### 解决什么问题？

| 问题 | 没有工作台时 | 有工作台后 |
|------|-------------|-----------|
| **AI 忘事** | 对话超过 10 轮，AI 忘了开头说的需求 | 所有决策都记录在节点里，随时查阅 |
| **过程黑盒** | 不知道 AI 做了什么、为什么这样做 | WebUI 实时展示任务树和执行日志 |
| **粒度失控** | AI 一次规划太多，执行质量下降 | 强制拆分，一次只做一个任务 |
| **难以继续** | 关掉对话就没了，下次从头来 | 工作区持久化，随时恢复进度 |

### 谁会受益？

- **你**：知道 AI 在做什么、做到哪了，能随时调整方向
- **AI**：有结构化的任务管理，不再迷失在长对话中
- **项目**：所有变更有迹可循，出问题能回溯

---

## 为什么选择 TanmiWorkspace？

市面上有多种 AI 编程辅助方案，TanmiWorkspace 的核心差异是什么？

### TanmiWorkspace 核心优势

#### 1. 系统强制 > 提示词引导

大多数方案用提示词「建议」AI 按流程工作——但 AI 可以忽略。

TanmiWorkspace 用四层机制「强制」AI 遵守：

```
状态机约束 ─→ AI 无法跳过规划直接执行
Hook 监控  ─→ 每次操作自动记录，阶段约束自动检查
Skill 流程 ─→ 同类任务强制走标准流程
派发系统   ─→ 执行与审查由不同 AI 完成，相互检查
```

**效果**：不是「希望」AI 遵守，而是系统不允许 AI 不遵守。

#### 2. 为复杂项目设计

| 能力 | 说明 |
|------|------|
| **树形任务结构** | 大任务自动拆分为多层子任务，进度层级聚合 |
| **完整持久化** | 工作区、节点、日志全部持久化，随时恢复 |
| **跨会话继续** | 关闭对话不丢失，下次说「继续」即可 |
| **WebUI 可视化** | 浏览器实时查看任务树和执行日志 |

#### 3. 唯一支持跨仓库

一个工作台管理多个项目/仓库的任务：

- 微服务架构（多个服务仓库）
- 前后端分离（前端仓库 + 后端仓库）
- 跨项目重构（涉及多个项目）

#### 4. 场景化规划

根据任务类型自动匹配最佳实践流程：

| 你说 | AI 识别为 | 自动应用流程 |
|------|----------|-------------|
| 「帮我加个搜索功能」 | Feature | 测试定义 → 功能实现 → 集成验证 |
| 「这里有个 bug」 | Debug | 问题复现 → 根因定位 → 修复验证 |
| 「页面加载太慢」 | Optimize | 基准测量 → 优化实现 → 效果对比 |
| 「帮我梳理一下架构」 | Summary | 范围拆分 → 逐项梳理 → 汇总输出 |

### 不适合的场景

TanmiWorkspace 也有不擅长的场景，请根据实际需求选择：

| 场景 | 为什么不适合 | 建议替代 |
|------|-------------|----------|
| 简单一次性小任务 | 管理开销可能超过收益 | 直接对话 |
| 需要 24/7 无人值守自动化 | TW 设计为人机协作，关键节点需确认 | CCCC |
| 追求极致轻量、零配置 | TW 需要安装和 setup | FeatureDev |
| 需要 IM 工具接收通知 | TW 目前不支持 Slack/Discord 桥接 | CCCC |

---

## 快速开始

### 安装

```bash
npm install -g tanmi-workspace
tanmi-workspace setup  # 自动检测环境并配置
```

**启动 WebUI 服务**（推荐）：

```bash
tanmi-workspace webui  # 启动后保持终端窗口打开
```

> **为什么要手动启动？** AI 助手启动时会自动启动 WebUI，后续打开的新对话会跳过启动共用服务，但关闭该对话窗口后 WebUI 也会停止，导致 WebUI 不可用。手动启动可以让 WebUI 独立运行，不受对话窗口影响。

启动后访问终端显示的地址（默认 `http://localhost:19540`）即可查看任务进度。

**其他管理命令**：
```bash
tanmi-workspace webui status  # 查看服务状态
tanmi-workspace webui stop    # 停止服务
tanmi-workspace webui restart # 重启服务
tanmi-workspace update        # 检查并更新到最新版本
```

端口被占用时可指定其他端口：`HTTP_PORT=19550 tanmi-workspace webui`

**支持的 AI 助手**：

| AI 助手 | 支持程度 | 说明 |
|---------|---------|------|
| **Claude Code** | 完全支持 | 所有功能可用，推荐使用 |
| **Cursor** | 完全支持 | 所有功能可用 |
| **OpenCode** | 完全支持 | 所有功能可用，开源替代方案 |
| **其他 MCP 兼容助手** | 基础功能 | 支持 MCP 即可使用核心功能 |

> **关于 OpenCode**：[OpenCode](https://opencode.ai/) 是一个开源的 AI 编码助手，类似 Claude Code 但完全开源。TanmiWorkspace 对 OpenCode 提供完整支持，包括 Hook 自动引导、Plugin 集成、Agent 模板和 Skill 流程。

其他智能体：只要支持 MCP 协议即可使用基础功能。如果智能体额外支持 subagent、skill 等特性，对应功能即可使用。配置方式需自行操作，可运行 `tanmi-workspace -h` 查看帮助。

### 第一次使用

**触发工作台的方式**：在对话中明确提及"**工作区**"或"**工作台**"。

```
你：用工作台帮我实现一个用户登录功能
```

或者：

```
你：创建一个工作区，帮我做用户登录功能
```

AI 会自动创建工作区并开始工作。下面是一个完整的对话示例：

---

**对话示例：实现登录功能**

```
你：用工作台帮我实现一个用户登录功能

AI：好的，我来创建工作区管理这个任务。

    📋 工作区已创建：用户登录功能
    🌐 WebUI 地址：http://localhost:19540/workspace/ws-xxx

    让我先了解一下你的项目情况...

    [AI 开始探索代码库]

    我看到你的项目是一个 Express + React 应用，已有用户模型。

    我制定了以下计划：
    1. 设计登录 API 接口
    2. 实现后端认证逻辑（JWT）
    3. 创建前端登录表单
    4. 添加登录状态管理

    确认开始执行吗？

你：好

AI：开始执行第一个任务：设计登录 API 接口

    [AI 开始工作...]

    ✅ 任务 1 完成：
    - 设计了 POST /api/auth/login 接口
    - 入参：email, password
    - 出参：token, user info

    继续执行任务 2：实现后端认证逻辑

    [继续工作...]

你：（可以随时在浏览器打开 WebUI 查看进度）

AI：🎉 所有任务已完成！

    总结：
    - 新增文件：src/routes/auth.js, src/components/Login.tsx
    - 修改文件：src/App.tsx, src/store/index.ts
    - 测试方式：访问 /login 页面

    还有其他需要吗？
```

---

**关键点**：

1. **你不需要学新命令**——用自然语言说任务就行
2. **AI 会主动创建工作区**——不需要你手动操作
3. **计划需要你确认**——AI 不会擅自开始执行
4. **过程可视化**——随时打开 WebUI 看进度

---

## 术语表

在深入使用之前，先了解几个核心概念。这些术语会在后文反复出现。

### 基础结构

| 术语 | 含义 |
|------|------|
| **工作台** | TanmiWorkspace 本身，管理所有工作区的平台 |
| **工作区** | 单个任务的完整记录，包含目标、规则和一棵任务树 |
| **节点** | 任务树中的每个任务单元，有类型、状态和内容 |

### 节点类型

每个节点有一个类型，决定它的用途：

| 类型 | 用途 | 典型场景 |
|------|------|----------|
| **planning**（规划） | 拆分和组织子任务 | "实现登录功能"拆成"设计接口"+"实现后端"+"实现前端" |
| **execution**（执行） | 实际执行工作 | 写代码、调研、测试 |

**关系**：规划节点像文件夹，执行节点像文件。一个规划节点下可以有多个子节点（规划或执行）。

**补充概念**：

| 术语 | 含义 |
|------|------|
| **MEMO** | 独立于节点树的草稿区，用于存储长内容（设计文档、调研结果等） |
| **引用** | 节点可以引用其他节点、MEMO 或外部文档，形成知识关联 |

MEMO 适合存放跨节点复用的内容，比如一份设计方案可能被多个执行节点引用。

**使用场景**：

| 场景 | 说明 | 示例 |
|------|------|------|
| **设计文档** | 较长的技术设计，不适合放在节点中 | 「登录模块设计方案」 |
| **调研结果** | 技术调研、竞品分析等 | 「Redis vs Memcached 对比」 |
| **跨节点复用** | 多个任务需要引用的公共信息 | 「API 接口规范」 |
| **长期规范** | 项目级别的规范和约定 | 「代码风格指南」「Git 提交规范」 |
| **会议记录** | 需求讨论、方案评审的记录 | 「2024-01-15 需求评审」 |

**创建示例**：

```
你：这个设计方案比较重要，单独记录一下

AI：好的，我创建一个 MEMO 来存放设计方案。

    📝 MEMO 已创建：用户认证设计方案

    内容包括：
    - 技术选型：JWT + Redis
    - 接口设计：/auth/login, /auth/refresh
    - 安全考虑：Token 过期策略

    已关联到当前节点。后续其他任务也可以引用这个 MEMO。
```

MEMO 由 AI 创建，可被多个节点引用。

### 节点状态

节点的生命周期通过状态流转表示：

**执行节点状态**：

| 状态 | 含义 | 下一步 |
|------|------|--------|
| **pending** | 等待开始 | AI 会按顺序执行，进入 implementing |
| **implementing** | 正在执行 | AI 完成后进入 completed，失败进入 failed |
| **completed** | 已完成 | 终态，任务结束 |
| **failed** | 执行失败 | 可以重试（reopen），重新进入 pending |

**规划节点状态**：

| 状态 | 含义 | 下一步 |
|------|------|--------|
| **pending** | 等待开始 | 开始规划，进入 planning |
| **planning** | 正在规划 | 子节点创建完成后，进入 monitoring |
| **monitoring** | 等待子任务完成 | 所有子节点完成后，进入 completed |
| **completed** | 所有子任务完成 | 终态 |
| **cancelled** | 已取消 | 终态，任务放弃 |

### 特殊节点角色

某些执行节点有特殊角色，用于特定用途：

| 角色 | 用途 | 何时出现 |
|------|------|----------|
| **info_collection**（信息收集） | AI 主动探索项目——扫描代码、读文档、理解架构 | 你没提供完整信息，AI 需要自己了解 |
| **info_summary**（信息总结） | AI 整理已有信息——从对话/你提供的设计方案中提取 | 你已提供完整设计方案或需求描述 |
| **dispatch_exec** | 派发任务的执行部分 | 启用派发模式时 |
| **dispatch_spec** | 派发任务的规格审查 | 启用派发模式时 |
| **dispatch_quality** | 派发任务的质量审查（可选） | 启用派发模式且开启质量审查时 |

**info_collection vs info_summary**：二选一。AI 会根据你提供的信息完整度决定用哪个：
- 你说"帮我实现搜索功能"→ AI 需要 info_collection，先探索代码库
- 你说"根据这个设计文档实现搜索功能"（附设计方案）→ AI 用 info_summary，整理你提供的信息

### 节点记录的信息

每个节点会记录以下内容，帮助追踪任务全貌：

| 字段 | 用途 | 何时记录 | 对话中如何触发 |
|------|------|----------|----------------|
| **requirement**（需求） | 任务描述、目标 | 创建节点时 | AI 自动记录，或你说"任务是..." |
| **acceptanceCriteria**（验收标准） | WHEN/THEN 格式的验收条件 | 意图对齐时 | AI 整理后记录 |
| **notes**（笔记） | 过程中的发现、决策 | 执行过程中 | 你说"记一下..."或 AI 主动记录 |
| **conclusion**（结论） | 完成时的总结 | 任务完成时 | AI 完成时必须填写 |
| **docs**（引用） | 关联的文档、MEMO、其他节点 | 任意时刻 | 你说"引用这个文档..." |
| **Log**（日志） | 操作记录（谁在何时做了什么） | 自动记录 | 关键操作自动记录 |
| **Problem**（问题） | 当前阻碍和下一步计划 | 遇到问题时 | 你说"卡住了"或 AI 主动记录 |

**查看方式**：
- **WebUI**：点击节点查看详情，包括需求、结论、日志、问题
- **对话中**：问 AI "这个节点的结论是什么"、"现在卡在哪里"

### 派发模式

处理复杂任务时的一种执行方式：

| 模式 | 特点 | 适用场景 |
|------|------|----------|
| **普通模式** | AI 直接执行，所有步骤在当前会话 | 简单任务、逻辑强依赖完整上下文的任务 |
| **派发模式** | 子任务派发给独立 Agent 执行，完成后审查 | 复杂任务、可独立执行的子任务 |

**派发节点状态**（仅在派发模式下）：

| 状态 | 含义 |
|------|------|
| **pending** | 等待执行 |
| **executing** | 子 Agent 正在执行 |
| **passed** | 审查通过 |
| **failed** | 审查失败，需重试 |

### 能力包

信息收集或信息总结阶段可选的功能模块，根据任务场景自动推荐：

| 能力 | 用途 |
|------|------|
| **intent_alignment** | 意图对齐——澄清需求，确认验收标准 |
| **context_discovery** | 上下文探索——扫描项目，理解架构 |
| **solution_design** | 方案设计——设计接口和实现路径 |
| **verification_strategy** | 验证策划——设计测试用例 |
| **diagnosis** | 问题诊断——定位 bug 根因 |
| **tech_research** | 技术调研——评估技术选型 |
| **measurement_analysis** | 度量分析——性能基准测试 |

### 其他概念

| 术语 | 含义 |
|------|------|
| **会话绑定** | AI 会话与工作区的关联，确保对话中的操作作用到正确的工作区 |
| **聚焦节点** | 当前正在操作的节点，AI 的工作上下文 |
| **WebUI** | 浏览器中的可视化界面，实时展示任务树和执行日志 |

---

## 完整使用流程

一个任务从开始到结束会经过 **3 个阶段**。了解这些阶段能帮你更好地与 AI 协作。

```
信息阶段 ──→ 规划阶段 ──→ 执行阶段
(收集/整理)   (分解任务)   (逐个实现)
```

### 阶段一：信息阶段

**触发方式**：在对话中明确提及"**工作区**"或"**工作台**"

| 示例 | 说明 |
|------|------|
| "用工作台帮我实现搜索功能" | 提及"工作台" |
| "创建一个工作区做这个任务" | 提及"工作区" |
| "帮我实现搜索功能" | ❌ 不会触发，需加上"工作台/工作区" |

**AI 会做什么**：

1. 创建工作区，告知 WebUI 地址
2. 推断任务场景（新功能/修复/优化/总结）
3. 展示推荐能力，让你选择需要执行的信息收集项
4. 执行选中的能力（意图对齐、上下文探索等）
5. 输出设计方案，等你确认

**你会看到**：

```
AI：好的，我来创建工作区管理这个任务。

    📋 工作区：搜索功能开发
    🌐 WebUI：http://localhost:19540/workspace/ws-xxx

    这是一个「新功能开发」任务，我将执行以下能力：
    - 意图对齐：澄清需求，确认验收标准
    - 上下文探索：扫描项目，理解架构

    还可以选择：
      a. 技术调研 - 评估技术选型
      b. 方案设计 - 设计接口和实现路径

    需要添加哪些？（输入 a/ab，或回车跳过）

你：目前即可，不需要添加

AI：好的，开始信息收集...

    [执行意图对齐]
    让我确认一下需求...

    [执行上下文探索]
    扫描项目结构...

    信息收集完成！设计方案如下：
    - 技术栈：Express + React
    - 搜索范围：标题、内容、标签
    - 接口设计：GET /api/v2/search?q=xxx

    是否进入规划阶段？
    - 输入「继续」进入规划
    - 输入需要补充的内容

你：继续
```

**两种信息模式**：

| 模式 | 触发条件 | AI 行为 |
|------|----------|---------|
| **信息收集** | 你只说了"帮我做XXX"/"和我讨论XXX" | AI 主动扫描项目、读文档、探索代码 |
| **信息总结** | 你已详细描述需求或提供设计方案 | AI 整理确认你提供的信息 |

### 阶段二：规划阶段

信息阶段完成后，AI 会将方案分解为具体的任务计划。

**根据场景类型，AI 会采用不同的规划模板**：

| 场景 | 规划流程 | 适用情况 |
|------|----------|----------|
| **Feature** | 测试定义 → 功能实现 → 集成验证 | 新功能开发 |
| **Debug** | 问题复现 → 根因定位 → 修复 → 回归 | Bug 修复 |
| **Optimize** | 基准测量 → 优化实现 → 效果验证 → 回归 | 性能优化 |
| **Summary** | 规模评估 → 分主题执行 → 完整性验证 | 总结分析 |
| **Misc** | 任务分析 → 分解规划 → 详细规划 | 其他任务 |

**你会看到**：

例如，在我们的例子中，AI 会根据设计方案，制定以下计划：

```
AI：根据设计方案，我制定了以下计划：

    [plan] 搜索功能开发
    ├── [exec] 测试定义 - 编写搜索接口测试
    │   验收：测试可运行但失败（红灯状态）
    ├── [plan] 功能实现
    │   ├── [exec] 实现后端搜索逻辑
    │   │   验收：GET /api/v2/search?q=xxx 返回结果
    │   └── [exec] 创建前端搜索组件
    │       验收：搜索框输入后显示结果列表
    └── [exec] 集成验证 - 确保所有测试通过
        验收：所有测试绿灯 + 无回归

    是否进入执行阶段？
    - 输入「继续」开始执行
    - 输入需要调整的内容

你：继续
```

**你可以**：

| 你说 | AI 会 |
|------|-------|
| "继续" / "好" | 进入执行阶段 |
| "第 2 个任务需要调整" | 修改规划 |
| "加一个分页功能" | 追加任务 |
| "这个任务太大了，拆分一下" | 细化分解 |

### 阶段三：执行阶段

规划确认后，AI 开始逐个执行任务。

**执行模式选择**（复杂任务时会询问）：

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| **全部派发** | 所有任务派发给独立 Agent | 任务独立、无需人工介入 |
| **智能派发** | 部分任务派发，敏感任务直接执行 | 部分任务需特殊处理/人工校验 |
| **不派发** | AI 直接执行全部 | 任务简单或需要连续上下文 |

**执行过程**：

1. **开始前**：标记任务为"进行中"
2. **执行时**：写代码、记录操作日志
3. **完成后**：逐条验证验收标准，填写结论

**你可以**：

```
你：做到哪了？

AI：当前进度：
    ✅ 测试定义（已完成）
    🔄 实现后端搜索逻辑（进行中）
    ○ 创建前端搜索组件（待开始）
    ○ 集成验证（待开始）
```

**WebUI 状态说明**：

*执行节点（方形图标）*：

| 状态 | 颜色 | 含义 |
|------|------|------|
| pending | 灰色空心 | 待执行 |
| implementing | 蓝色斜纹 | 执行中 |
| validating | 橙色实心 | 验证中 |
| completed | 黑色实心 | 已完成 |
| failed | 红底白叉 | 失败 |

*规划节点（菱形图标）*：

| 状态 | 颜色 | 含义 |
|------|------|------|
| pending | 灰色空心 | 待执行 |
| planning | 紫色横纹 | 规划中 |
| monitoring | 蓝框中心点 | 监控子任务 |
| completed | 黑色实心 | 已完成 |
| cancelled | 灰色虚线 | 已取消 |

**所有任务完成后，AI 会汇报**：

```
AI：🎉 搜索功能开发完成！

    完成情况：
    ✅ 测试定义 - 编写了 SearchService 单元测试
    ✅ 实现后端逻辑 - 支持关键词搜索和分页
    ✅ 创建前端组件 - SearchBox + SearchResults
    ✅ 集成验证 - 所有测试通过，无回归

    新增文件：
    - src/api/v2/search.ts
    - src/components/Search/index.tsx

    测试方式：npm test && npm run dev

    还有其他需要吗？
```

**你可以**：

- 确认完成，结束任务
- 提出新需求："再加一个搜索历史功能"
- 报告问题："搜索结果没有高亮关键词"

---

## 如何保证 AI 行为

你可能会问：AI 真的会按流程做事吗？会不会跳步骤、忘记记录、自己验证自己？

TanmiWorkspace 用四层机制确保 AI "不得不"按规矩来：

### 状态机：物理级约束

普通 AI 助手全靠"自觉"——你说"先规划再执行"，AI 可能直接开始写代码。

TanmiWorkspace 不信任自觉，用**状态转换表**强制约束：

| 可能的问题 | 常见Spec | TanmiWorkspace |
|-------------|---------|----------------|
| AI 跳过规划直接执行 | 可能发生 | 不可能：节点必须从 pending→implementing，跳不过 |
| 任务没完成就说"完成了" | 靠自觉 | 不可能：complete 需要填写 conclusion，系统验证 |
| 同时做多个任务导致混乱 | 可能发生 | 不可能：同级节点只能有一个在执行中 |
| 子任务没做完就汇总 | 可能发生 | 不可能：父节点 complete 前会检查所有子节点状态 |

**实际效果**：AI 尝试非法操作时，系统直接拒绝并返回错误提示，而不是"希望"AI 遵守。

### 自动监视与引导

每次 AI 调用工具（修改代码、执行命令、操作节点），系统都会自动：

1. **记录**：在日志中追加"谁在何时做了什么"
2. **引导**：返回下一步必须做什么

| 可能的问题 | 常见Spec | TanmiWorkspace |
|-------------|---------|----------------|
| AI 改了代码但没记录 | 经常发生 | 不可能：Hook 自动拦截每个修改操作并记录 |
| AI 不知道下一步做什么 | "完成了，接下来呢？" | 系统自动返回 actionRequired，精确指导 |
| AI 在多个任务间跳来跳去 | 聚焦点漂移 | Hook 自动聚焦到最后操作的节点 |

### Skill：标准化流程

市面上的 AI 助手用提示词引导行为，但提示词是建议，AI 可以忽略。

TanmiWorkspace 的 Skill 是**强制性 SOP**：

- **信息收集**：必须按"定位入口→分析依赖→追踪数据流→输出快照"的流程
- **意图对齐**：必须产出 WHEN/THEN 格式的验收标准
- **任务执行**：必须按"评估→计划→执行→验证→交付"五步走
- **规格审查**：必须逐条验证验收标准，任何一条失败=整体失败

| 可能的问题 | 常见Spec | TanmiWorkspace |
|-------------|---------|----------------|
| 每次执行方式不一致 | 看心情 | 同类任务强制走相同流程 |
| 说"完成了"但没验证 | 靠自觉 | Skill 强制：执行→验证→记录→完成，缺一不可 |
| 验证走过场 | 自己检查自己 | 审查 Skill 要求逐条验证，附带证据 |

### 派发系统：执行与审查分离

这是 TanmiWorkspace 核心设计之一：**让不同的 AI 相互检查**。

常见Spec是"自己做自己查"——就像考试自己批卷子，很难客观。

派发模式下，系统会启动多个独立 AI：

| 角色 | 职责 | 约束 |
|------|------|------|
| **执行者**（tanmi-executor） | 写代码、实现功能 | 只能改需求范围内的代码，完成后提交审查 |
| **规格审查**（tanmi-reviewer） | 验证是否符合需求 | **不能修改代码**，必须独立验证，不信任执行者的结论 |
| **质量审查**（tanmi-reviewer） | 检查代码质量 | **不能修改代码**，检查可读性、安全性、性能 |

| 可能的问题 | 常见Spec | TanmiWorkspace 派发模式 |
|-------------|---------|------------------------|
| 自己做自己查 | 总是这样 | 执行与审查由不同 AI 完成 |
| 审查倾向于通过 | 利益冲突 | 审查者无法修改代码，只能判 pass/fail |
| 失败后代码残留 | 手动回滚 | Git 模式自动回滚到执行前 |
| 重试没有方向 | 从头再来 | 失败原因会传递给下一次执行 |

### Hook 系统：自动记录与引导

Hook 是在 AI 工作流程关键节点自动触发的检查和引导机制，无需用户干预。

**Hook 事件类型**：

| 事件 | 触发时机 | 用途 |
|------|---------|------|
| **SessionStart** | 会话启动/恢复 | 自动注入工作区上下文，引导调用阶段 Skill |
| **UserPromptSubmit** | 用户提交消息前 | 检测工作区关键词，建议绑定相关工作区 |
| **PreToolUse** | 工具执行前 | 权限检查、阶段约束验证 |
| **PostToolUse** | 工具执行后 | 错误检测、自动提醒记录变更 |

| 机制 | 说明 | 效果 |
|------|------|------|
| **写操作限制** | 未绑定工作区时，34 个写操作工具受限 | 防止误修改工作区数据 |
| **阶段工具约束** | info/design 阶段禁止 Write/Edit 等工具 | 确保先规划再动手 |
| **流程强制** | 必须调用阶段 Skill 才能继续其他操作 | AI 不能跳过工作流阶段 |
| **智能提醒** | 执行 Bash/Edit 后自动提醒记录到工作区 | 确保变更有迹可循 |

**实际效果**：

```
# AI 尝试在信息阶段直接写代码
AI：[调用 Write 工具]
Hook：❌ 当前处于信息阶段，禁止写入文件。请先完成信息收集并进入执行阶段。

# AI 执行 Bash 命令出错
AI：[Bash 执行失败]
Hook：💡 检测到命令执行失败，建议使用 problem_update 记录问题。

# AI 忘记绑定工作区就想修改
AI：[调用 node_edit]
Hook：⚠️ 当前会话未绑定工作区，写操作已被限制。请先执行 session_bind。
```

### 为什么这套机制有效？

核心哲学：**不依赖 AI 的"自觉"，用系统强制确保合规**。

```
常见Spec流程的信任链：
  用户 → 相信 AI → AI 自觉遵守 → 结果

TanmiWorkspace 的信任链：
  用户 → 系统强制 → AI 不得不遵守 → 结果
         ↑
    状态机拒绝非法操作
    Hook 自动记录和引导
    Skill 强制标准流程
    派发分离执行与审查
```

**你能感知到的变化**：

- 打开 WebUI，每个节点都有完整的日志，知道 AI 做了什么
- AI 不会跳过中间流程，必须一步步走完流程
- 派发模式下，你能看到执行、审查、质量检查各自独立的记录
- 出问题时，能追溯到具体哪一步出了问题

---

## 场景指南

不同类型的任务有不同的最佳实践。v1.11.0 引入了场景化规划模板，帮助你更好地完成各类任务。

### 快速参考：如何选择场景？

| 你的任务是 | 选择场景 | 核心流程 |
|-----------|---------|---------|
| 实现新功能、添加新模块 | **Feature** | 先写测试，再实现，最后验证 |
| 修复 bug、解决报错 | **Debug** | 复现问题，找根因，修复，验证 |
| 加快性能、优化体验 | **Optimize** | 建立基准，优化，对比效果 |
| 梳理架构、生成文档 | **Summary** | 拆分主题，完整覆盖，汇总输出 |
| 重构代码、开发工具、其他 | **Misc** | 完整分解，逐个执行 |

### 场景 1：新功能开发（Feature）

**适用于**：添加新功能、实现新特性、创建新模块

**推荐流程**：测试驱动开发（TDD）

```
1️⃣ 测试定义 → 2️⃣ 功能实现 → 3️⃣ 集成验证
   (红灯)         (绿灯)        (无回归)
```

> **大功能分期**：功能规模大时，分为多期实现，每期独立走一遍"测试→实现→验证"流程。

**情况 A：你只说了要做什么（AI 需要探索项目）**

```
你：用工作台帮我给博客系统加一个评论功能

AI：好的，我来创建工作区管理这个任务。

    📋 工作区：博客评论功能
    🌐 WebUI：http://localhost:19540/workspace/ws-abc123

    让我先了解一下你的博客系统...

    [探索代码库]

    我看到：
    - 后端：Express + MongoDB
    - 前端：React + TailwindCSS
    - 已有文章模型 (Post)

    我需要确认几个问题：
    1. 评论需要用户登录吗？
    2. 是否需要嵌套回复？

你：需要登录，暂时不需要嵌套回复

AI：明白了，我制定了以下计划...
```

**情况 B：你提供了完整设计方案（AI 只需整理确认）**

```
你：用工作台帮我做评论功能，设计如下：
    - 数据模型：Comment { content, authorId, postId, createdAt }
    - API：POST/GET /posts/:id/comments, DELETE /comments/:id
    - 前端：CommentList + CommentInput 组件
    - 需要登录才能评论

AI：好的，我来整理一下你的设计方案。

    📋 工作区：博客评论功能
    🌐 WebUI：http://localhost:19540/workspace/ws-abc123

    ## 需求确认

    | WHEN | THEN |
    |------|------|
    | 用户提交评论 | 调用 POST API，评论显示在列表 |
    | 未登录用户访问 | 隐藏评论输入框 |
    | 删除评论 | 调用 DELETE API，列表刷新 |

    设计方案清晰，确认开始执行吗？

你：好

AI：开始执行...
```

### 场景 2：Bug 调试修复（Debug）

**适用于**：修复 bug、解决报错、排查问题

**推荐流程**：诊断驱动修复

```
1️⃣ 问题复现 → 2️⃣ 根因定位 → 3️⃣ 修复验证
   (确认现象)     (诊断分析)     (回归测试)
```

> **复杂问题**：根因不明确时，可能需要多轮"假设→验证"的诊断循环。

**情况 A：你只描述了现象（AI 需要诊断）**

```
你：用工作台帮我查一下，用户登录后刷新页面会被踢出

AI：好的，这是一个 Bug 修复任务。让我创建工作区。

    📋 工作区：登录状态丢失修复
    🌐 WebUI：http://localhost:19540/workspace/ws-def456

    让我先诊断问题...

    [分析认证相关代码]

    我找到了原因：AuthContext.tsx 中没有验证 token 是否过期，
    当 token 过期时 decodeToken 返回 null，导致用户被踢出。

    修复方案：添加 token 过期检查

    确认这个修复方案吗？

你：好

AI：开始修复...
```

**情况 B：你已经定位了问题原因（AI 只需确认方案）**

```
你：用工作台帮我修一个 bug：
    - 现象：登录后刷新会被踢出
    - 原因：AuthContext.tsx 的 useEffect 没有验证 token 过期
    - 方案：添加 isTokenExpired() 检查，过期则跳转登录页

AI：好的，我来整理一下你的修复方案。

    📋 工作区：登录状态丢失修复
    🌐 WebUI：http://localhost:19540/workspace/ws-def456

    ## 修复确认

    | WHEN | THEN |
    |------|------|
    | token 有效 | 正常加载用户信息 |
    | token 过期 | 显示"登录已过期"，跳转登录页 |

    方案清晰，确认开始修复吗？

你：好

AI：开始修复...
```

### 场景 3：性能优化（Optimize）

**适用于**：性能优化、加载提速、资源优化

**推荐流程**：数据驱动优化

```
1️⃣ 建立基准 → 2️⃣ 实施优化 → 3️⃣ 效果对比
   (量化数据)     (具体措施)     (验证提升)
```

> **多项优化**：分开实施，每项单独测量效果，避免效果混淆。

**情况 A：你只说了要优化（AI 需要分析性能）**

```
你：用工作台帮我优化一下，首页加载太慢了

AI：好的，这是一个性能优化任务。让我创建工作区。

    📋 工作区：首页性能优化
    🌐 WebUI：http://localhost:19540/workspace/ws-ghi789

    让我先分析性能瓶颈...

    [分析性能]

    当前数据：首屏 4.2s，JS 包 1.8MB
    问题：没有代码分割，图表库未按需加载

    优化计划：
    1. 配置路由级代码分割
    2. 动态导入图表库
    3. 添加图片懒加载

    确认开始优化吗？

你：好

AI：开始优化...
```

**情况 B：你已经提供了性能报告（AI 只需确认方案）**

```
你：用工作台优化首页性能，我的分析：
    - 当前：首屏 4.2s，JS 1.8MB
    - 问题：bundle 没分割，chart.js 未按需加载
    - 方案：路由分割 + 动态导入 + 图片懒加载

AI：好的，我来整理一下你的优化方案。

    📋 工作区：首页性能优化
    🌐 WebUI：http://localhost:19540/workspace/ws-ghi789

    ## 优化确认

    | 优化项 | 预期效果 |
    |--------|----------|
    | 路由级代码分割 | 首页 JS 减少 50% |
    | 动态导入图表库 | 首页不再包含 chart.js |
    | 图片懒加载 | 首屏只加载可见图片 |

    方案清晰，确认开始优化吗？

你：好

AI：开始优化...
```

### 场景 4：文档/总结任务（Summary）

**适用于**：代码分析、文档生成、技术调研、架构梳理

**推荐流程**：分解覆盖

```
1️⃣ 范围拆分 → 2️⃣ 逐项梳理 → 3️⃣ 汇总输出
   (主题分解)     (完整覆盖)     (生成成果)
```

> **大范围梳理**：先列出所有主题，确保不遗漏，再逐个深入。

**情况 A：你需要 AI 探索代码库**

```
你：创建一个工作区，帮我梳理一下这个项目的架构，我要写技术文档

AI：好的，这是一个总结分析任务。让我创建工作区。

    📋 工作区：项目架构梳理
    🌐 WebUI：http://localhost:19540/workspace/ws-jkl012

    让我分析项目结构...

    [扫描项目、读取文档、分析代码]

    ## 项目架构总结

    ### 技术栈
    | 层级 | 技术选型 |
    |------|----------|
    | 前端 | React + Redux + TailwindCSS |
    | 后端 | Express + Sequelize |
    | 数据库 | PostgreSQL |
    | 部署 | Docker + Nginx |

    ### 目录结构
    src/
    ├── api/          # 后端 API
    ├── web/          # 前端应用
    ├── shared/       # 共享代码
    └── scripts/      # 工具脚本

    ### 核心模块
    1. 用户系统 - 注册、登录、权限
    2. 商品系统 - CRUD、分类、搜索
    3. 订单系统 - 下单、支付、物流
    4. 后台管理 - 统计、内容管理

    需要我把这些内容生成 ARCHITECTURE.md 文档吗？

你：好

AI：[生成文档]

    ✅ 已创建 docs/ARCHITECTURE.md

    文档包含：
    - 项目概述
    - 技术栈说明
    - 目录结构详解
    - 核心模块说明
    - 数据流图
    - 部署架构

    需要补充其他内容吗？
```

**情况 B：你已经提供了项目说明（AI 只需整理成文档）**

```
你：用工作台帮我生成架构文档，项目信息如下：
    - 技术栈：Express + React + PostgreSQL
    - 目录：api/(后端), web/(前端), shared/(共享)
    - 模块：用户、商品、订单、后台

AI：好的，我来整理你提供的项目信息。

    📋 工作区：项目架构梳理
    🌐 WebUI：http://localhost:19540/workspace/ws-jkl012

    ## 信息确认

    根据你的描述，项目架构如下：
    - 三层架构：前端 + 后端 + 共享
    - 四大模块：用户、商品、订单、后台

    还有需要补充的信息吗？（如部署方式、数据流等）

你：没了，直接生成

AI：[生成文档]

    ✅ 已创建 docs/ARCHITECTURE.md
```

### 场景 5：其他任务（Misc）

**适用于**：代码重构、工具开发、环境配置、不属于以上类型的杂项任务

**推荐流程**：完整分解

```
1️⃣ 任务分解 → 2️⃣ 逐个执行 → 3️⃣ 汇总验证
   (明确边界)     (循序渐进)     (检查完整)
```

> **复杂重构**：先梳理依赖关系，按依赖顺序逐步重构，每步验证不破坏现有功能。

```
你：用工作台帮我把项目的 ESLint 从 v8 升级到 v9

AI：好的，这是一个工具配置任务。让我创建工作区。

    📋 工作区：ESLint v9 升级
    🌐 WebUI：http://localhost:19540/workspace/ws-mno345

    让我分析升级影响...

    [分析配置文件和依赖]

    升级涉及：
    1. eslint.config.js 格式变化（flat config）
    2. 部分插件需要更新版本
    3. 部分规则名称变化

    任务拆分：
    - [ ] 更新 ESLint 和相关插件版本
    - [ ] 迁移配置到 flat config 格式
    - [ ] 修复规则名称变化
    - [ ] 运行 lint 验证

    确认开始升级吗？

你：好

AI：开始升级...
```

---

## 最佳实践

以下是从实际使用中总结的最佳实践，帮助你更高效地使用 TanmiWorkspace。

### 任务拆分的原则

**好的拆分**：每个子任务独立、可验证、粒度适中

| 原则 | 说明 | 示例 |
|------|------|------|
| **独立性** | 子任务之间尽量不相互依赖 | ✅ 「前端组件」和「后端接口」可独立开发 |
| **可验证** | 每个子任务有明确的验收标准 | ✅ 「接口返回正确数据」 |
| **粒度适中** | 不要太大（难以管理）也不要太小（开销过大） | ✅ 2-4 小时工作量 / 子任务 |

**拆分示例**：

```
❌ 不好的拆分（太粗）：
1. 实现用户系统

✅ 好的拆分：
1. 用户注册
   - 注册表单组件
   - 注册接口
   - 邮箱验证
2. 用户登录
   - 登录表单组件
   - 认证接口
   - Token 管理
3. 用户信息
   - 个人资料页
   - 修改密码
```

### 何时使用派发模式

| 任务特点 | 建议 | 理由 |
|----------|------|------|
| 子任务可独立执行 | ✅ 用派发 | 充分发挥并行和审查优势 |
| 需要上下文强连贯 | ❌ 不用派发 | 派发会丢失对话上下文 |
| 代码修改任务 | ✅ 推荐派发 | 自动审查 + Git 回滚保护 |
| 文档/总结任务 | ✅ 可用派发 | 分片处理后汇总 |
| 调试/排查任务 | ⚠️ 谨慎使用 | 调试需要连续上下文，可只派发验证环节 |

### 信息收集 vs 信息总结

| 你的情况 | AI 会使用 | 你应该 |
|----------|----------|--------|
| 只说了要做什么，没给细节 | 信息收集（探索代码库） | 让 AI 先了解项目 |
| 已经有完整设计方案 | 信息总结（整理你的输入） | 直接提供方案，跳过探索 |
| 有部分想法，需要补充 | 信息收集 + 你的输入 | 说明已有想法，让 AI 补充 |

**示例**：

```
# 信息收集模式（AI 需要探索）
你：用工作台帮我加个搜索功能

# 信息总结模式（你已有方案）
你：用工作台帮我加搜索功能，设计如下：
    - 接口：GET /api/search?q=xxx
    - 返回：{ results: [], total: number }
    - 前端：SearchBox + ResultList 组件
```

### 长期项目的管理策略

对于持续数周的长期项目，建议：

1. **按阶段创建工作区**
   - 不要把整个项目放一个工作区
   - 按功能模块或迭代周期分工作区
   - 例：「v1.0-用户系统」「v1.0-订单系统」「v1.1-性能优化」
   - 可以创建管理工作区的工作区

2. **定期归档已完成工作区**
   - 完成的工作区及时归档
   - 保持活跃工作区数量可控

3. **使用 MEMO 记录长期决策**
   - 架构决策、技术选型等长期有效的内容
   - 放在 MEMO 而非节点中，便于跨工作区引用

4. **暂停前充分记录**
   - 长期项目难免中断
   - 暂停前让 AI 记录当前状态和下一步计划

---

## 进阶功能

掌握了基础流程后，以下进阶功能能让你更高效地使用 TanmiWorkspace。

### 继续之前的任务

关掉对话后，下次开启新对话时，建议让 AI **先了解状态再决定下一步**：

```
你：看看工作区的状态

AI：📋 博客评论功能 (ws-abc123)

    整体进度：2/3 任务已完成

    任务状态：
    ✅ 任务 1：创建评论数据模型（已完成）
    ✅ 任务 2：实现评论 API（已完成）
    🔄 任务 3：创建评论组件（进行中）

    当前聚焦：任务 3 - 创建评论组件
    上次停在：组件结构设计阶段

你：继续任务 3

AI：好的，让我先回顾一下任务 3 的进展...

    [查看节点日志和已完成内容]

    我看到上次已经完成了：
    - 组件目录结构
    - CommentList 组件骨架

    接下来需要：
    - 实现评论输入框
    - 添加样式

    继续吗？
```

**推荐的恢复流程**：

1. **先查看状态**："看看工作区状态" / "进度怎么样了"
2. **确认聚焦点**：AI 会告诉你当前在哪个节点
3. **决定下一步**：继续当前任务 / 切换到其他任务

**派发模式下的恢复**：

如果之前在使用派发模式，对话清空后 AI 可能丢失派发上下文。建议：

```
你：看看工作区状态，我之前在用派发模式

AI：📋 博客评论功能 (ws-abc123)

    派发状态：已启用
    派发母节点：任务 3 - 创建评论组件

    派发子节点状态：
    - exec（执行）：已完成
    - spec（规格审查）：进行中
    - quality（质量审查）：待开始

    需要我继续派发流程吗？

你：好

AI：回到派发母节点，继续规格审查...
```

> **提示**：如果 AI 对派发状态理解不清晰，可以说"回到派发母节点，重新开始派发流程"让 AI 重新构建派发上下文。

**上下文自动压缩时的处理**：

对话过长时，AI 助手会自动压缩上下文（类似清空对话）。这时可能出现 AI "失忆"的情况，需要观察和提醒：

```
你：（发现 AI 好像忘了之前讨论的内容）
    你之前在做什么？看看工作区状态

AI：让我查看一下...
    [读取工作区和节点信息]

    我看到当前在任务 3，之前的进展是...
```

**降低上下文压缩风险的方法**：

- **分段式完成任务**：将大任务拆分为独立阶段（调研 → 规划 → 执行 → 总结），每个阶段完成后记录结论再进入下一阶段
- **使用派发模式**：复杂任务用派发模式执行，子 Agent 独立运行，不占用主会话上下文
- **及时记录关键信息**：让 AI 把重要决策和发现记录到节点的 notes 或 conclusion 中

### 追加需求 / 重开任务

任务完成后想追加功能：

```
你：（任务已完成）还想加一个评论点赞功能

AI：好的，我来追加这个需求。

    在当前工作区新增任务：
    4. [执行] 实现评论点赞
       - 后端 API：POST /api/comments/:id/like
       - 前端显示点赞数和点赞按钮

    确认开始吗？
```

任务失败后想重试：

```
你：重新做一遍

AI：好的，我把任务 3 重新打开。

    当前状态：implementing（进行中）

    之前的失败原因是：组件样式冲突

    这次我会：
    1. 先检查现有样式
    2. 使用 CSS Module 避免冲突

    开始重试？
```

新需求较复杂时，让 AI 重新收集信息：

```
你：还想加一个评论审核功能，这个比较复杂，先收集一下信息

AI：好的，这是一个较复杂的新需求，我先进行信息收集。

    [创建信息收集节点]

    让我了解一下：
    1. 审核流程是怎样的？（自动 / 人工 / 混合）
    2. 审核状态有哪些？（待审核 / 通过 / 拒绝）
    3. 谁有审核权限？
    4. 是否需要敏感词过滤？

你：人工审核，管理员有权限，需要敏感词过滤

AI：明白了，让我再看看现有的权限系统...

    [探索代码库]

    信息收集完成，我制定了以下计划：

    1. [执行] 添加评论状态字段
    2. [执行] 实现敏感词过滤
    3. [执行] 创建审核 API
    4. [执行] 添加管理后台审核页面

    确认开始吗？
```

> **提示**：复杂的新需求走一遍信息收集流程，能让 AI 更好地理解需求并制定合理计划，避免遗漏关键点。

### 派发模式（适合复杂任务）

对于复杂任务，AI 可以把子任务派发给专门的执行器，实现"执行与审查分离"。

**派发模式 vs 普通模式**：

| 对比 | 普通模式 | 派发模式 |
|------|----------|----------|
| 执行者 | 主 AI 直接执行 | 专门的 executor 执行 |
| 上下文 | 完整对话上下文 | 只有当前任务上下文 |
| 审查 | 无 | 自动规格审查（三节点流程） |
| 回滚 | 手动 | 自动（Git 模式） / 手动（NoGit 模式） |
| 适合 | 简单任务、强依赖完整上下文的任务 | 复杂任务、可独立执行的子任务 |

**什么时候用派发模式**：

| 任务类型 | 建议 | 原因 |
|----------|------|------|
| 中大规模代码任务 | ✅ 推荐派发 | 可拆分为独立单元执行 |
| 整理/梳理任务 | ✅ 推荐派发 | 用派发整理局部结论，再集中总结 |
| 小规模任务 | ❌ 不推荐 | 派发开销可能超过收益 |
| Bug 修复 | ⚠️ 部分使用 | 上下文强相关，可只用派发做验证环节 |

**简单判断**：如果任务能拆成多个"互不依赖"的子任务，就适合派发。

#### 三节点派发流程

派发模式采用"三节点"架构，确保执行质量：

```
任务节点（母节点）
    ↓ dispatch_node 升级
    ├── exec（执行节点）→ tanmi-executor 执行
    ├── spec（规格审查）→ tanmi-reviewer 验证
    └── quality（质量审查，可选）→ tanmi-reviewer 检查
```

| 节点 | 执行者 | 职责 |
|------|--------|------|
| **exec** | tanmi-executor | 实现功能、修改代码 |
| **spec** | tanmi-reviewer | 核对验收标准是否满足 |
| **quality** | tanmi-reviewer | 检查代码质量（可选） |

流程保障：
- exec 失败 → 不会触发 spec 审查，直接分析原因重试或升级
- spec 失败 → 明确指出未满足的验收标准，重新执行 exec
- 三方独立 → 执行者不审查自己的代码

#### Git 模式 vs NoGit 模式

派发支持两种运行模式：

| 对比 | Git 模式 | NoGit 模式 |
|------|----------|------------|
| Git 操作 | 自动创建分支、合并 | 不操作 Git |
| 失败回滚 | 自动回滚 | 需手动回滚 |
| 前置要求 | 工作区干净、无未提交变更 | 无 |
| 适用场景 | 代码变更任务 | 文档生成、非代码任务 |
| 稳定性 | ⚠️ 实验功能 | ✅ 稳定 |

> **提示**：系统默认使用 NoGit 模式。如需 Git 模式，可通过 WebUI 设置或告诉 AI "启用 Git 派发模式"。

**对话示例**：

```
你：这个任务比较复杂，用派发模式

AI：好的，启用派发模式。

    派发模式说明：
    - 每个子任务由独立的 executor 执行
    - 执行完成后会有 reviewer 审查结果
    - 失败时自动回滚代码变更（Git 模式）

    当前任务会被派发为：
    1. 执行器执行 → 2. 规格审查 → 3. 质量审查（可选）

    确认启用？

你：好

AI：派发模式已启用。

    开始派发任务 1 给 tanmi-executor...

    [executor 独立执行]

    ✅ 执行完成
    开始规格审查...

    [tanmi-reviewer 检查结果]

    ✅ 规格审查通过
    - 所有验收标准满足
    - 代码变更已合并

    继续派发下一个任务...
```

#### 质量审查（可选）

质量审查用于检查代码可读性、安全性、性能等，适合以下场景：

| 场景 | 建议 |
|------|------|
| 代码质量要求高 | ✅ 开启 |
| 重构/优化任务 | ✅ 开启 |
| 快速迭代/原型开发 | ❌ 可不开启，节省时间 |

AI 默认倾向不开启。你可以通过对话告知 AI：
- "这个任务开启质量审查"
- "最后一个子任务做质量审查"
- "重构相关的任务都要质量审查"

#### 成本与注意事项

- **时间消耗**：派发模式为每个子任务构建独立上下文并进行完整的实现—验证流程，时间消耗比普通模式更高。适合用在复杂或中大型任务上。
- **自动回滚**：是 **Git 模式专属**功能，通过 Git 分支管理实现。

> ⚠️ **注意**：Git 模式涉及 Git 操作，属于**实验功能**。使用前建议做好代码备份。

### 多工作区管理

同时进行多个任务时：

```
你：我现在有几个工作区？

AI：你有 3 个工作区：

    1. 📋 博客评论功能 (ws-abc123)
       状态：进行中，2/3 任务完成

    2. 📋 首页性能优化 (ws-ghi789)
       状态：已完成

    3. 📋 用户登录修复 (ws-def456)
       状态：已完成

你：切换到评论功能

AI：已切换到工作区：博客评论功能

    当前进度：
    ✅ 任务 1：创建评论数据模型
    ✅ 任务 2：实现评论 API
    🔄 任务 3：创建评论组件（进行中）

    继续执行任务 3 吗？
```

### 跨仓库任务管理

TanmiWorkspace 是唯一支持跨多个代码仓库管理任务的方案。

**适用场景**：

| 场景 | 说明 |
|------|------|
| 微服务架构 | 一个功能涉及多个服务仓库（用户服务、订单服务、网关等） |
| 前后端分离 | 前端仓库和后端仓库需要协同修改 |
| 跨项目重构 | 抽取公共库，多个项目需要同步调整 |

**使用方式**：

```
你：用工作台帮我做一个跨服务的用户认证功能，涉及：
    - 前端仓库：~/projects/web-app
    - 用户服务：~/projects/user-service
    - 网关服务：~/projects/api-gateway

AI：好的，我来创建工作区管理这个跨仓库任务。

    📋 工作区：跨服务用户认证
    🌐 WebUI：http://localhost:19540/workspace/ws-xxx

    我识别到这是一个跨仓库任务，涉及 3 个仓库。

    让我先了解各仓库的情况...

    [探索各仓库结构]

    计划如下：
    1. [plan] 前端认证模块
       ├── [exec] 登录页面组件
       └── [exec] Token 管理
    2. [plan] 用户服务改造
       ├── [exec] 认证接口实现
       └── [exec] Token 签发逻辑
    3. [plan] 网关配置
       └── [exec] 认证中间件
    4. [exec] 集成测试

    确认开始吗？

你：好

AI：开始执行...

    当前工作在：~/projects/web-app
    [执行前端任务]

    切换到：~/projects/user-service
    [执行后端任务]

    ...
```

### 工作区管理

**归档与删除**

完成的工作区建议**归档**而非删除：

```
你：把这个工作区归档
AI：已归档工作区：博客评论功能
```

| 处理方式 | 说明 |
|----------|------|
| **归档**（推荐） | 不会被 AI 直接查询到，提高巡检效率；可供后期复盘 |
| **删除** | 不建议删除流程完备的工作区，会丢失执行记录 |

> **提示**：归档的工作区仍保留在 `.tanmi-workspace/` 目录中，需要时可以恢复。

**暂停与恢复**

如果需要暂停任务，不建议直接关闭对话。更好的做法：

```
你：我要离开了，把当前进度记录一下
AI：好的，我来记录当前状态...

    [记录到节点 notes/conclusion]

    已记录：
    - 当前在：任务 3 - 创建评论组件
    - 进度：组件结构已完成，样式待实现
    - 下一步：添加 CSS Module 样式

    下次继续时说"看看工作区状态"即可恢复。
```

**原因**：直接关闭可能导致执行中的节点状态不完整。让 AI 先记录上下文，后续恢复更顺畅。

**导入导出**

工作区支持导出为 `.twsp` 文件，便于备份、迁移或分享：

| 操作 | 入口 | 说明 |
|------|------|------|
| **导出** | 工作区详情页 →「EXPORT」按钮 | 导出为 .twsp 文件，包含完整工作区数据 |
| **导入** | 设置 → 索引管理 → 管理 | 拖拽 .twsp 文件或目录，自动扫描导入 |

导入支持三种方式：
- 拖拽 `.twsp` 文件直接导入
- 拖拽项目目录自动扫描
- 手动输入路径

> **提示**：导入功能还可以扫描已索引路径，发现新工作区并清理无效条目。

**备份与恢复**

工作台支持全局备份，便于版本升级前保护数据或迁移环境：

| 操作 | 入口 | 说明 |
|------|------|------|
| **创建备份** | 设置 → 备份管理 → 创建备份 | 备份所有工作区数据为 .twbak 文件 |
| **恢复备份** | 设置 → 备份管理 → 选择备份 → 恢复 | 从备份恢复工作区数据 |
| **导出备份** | 设置 → 备份管理 → 导出 | 导出备份文件用于迁移 |
| **导入备份** | 设置 → 备份管理 → 导入 | 导入外部备份文件 |

> **提示**：执行 `tanmi-workspace update --beta` 更新到测试版本时，系统会自动创建备份。

### 查看和导出日志

每个任务的执行过程都会被记录：

```
你：看看任务 2 的执行日志

AI：任务 2 执行日志：

    [10:32:15] 开始执行：实现评论 API
    [10:32:20] 分析现有 API 结构
    [10:33:05] 创建 src/routes/comments.js
    [10:33:45] 实现 POST /api/posts/:id/comments
    [10:34:30] 实现 GET /api/posts/:id/comments
    [10:35:10] 实现 DELETE /api/comments/:id
    [10:35:50] 添加路由注册
    [10:36:00] 完成：评论 API 实现完成

你：导出整个工作区的日志

AI：日志已导出到：.tanmi-workspace/博客评论功能_ws-abc123/export.md
```

### WebUI 功能

访问 `http://localhost:19540/workspace/<工作区ID>` 可以：

| 功能 | 说明 |
|------|------|
| 任务树视图 | 树状展示所有节点，颜色区分状态 |
| 节点详情 | 点击节点查看需求、结论、日志 |
| 进度统计 | 各状态节点数量，完成百分比 |
| MEMO 查看 | 查看 AI 记录的备忘信息 |
| 实时更新 | 自动刷新，无需手动刷新 |

> **注意**：WebUI 仅支持本机访问（localhost）。如需远程访问，可通过端口映射工具实现。

### OpenSpec 导入（实验功能）

如果你的项目使用了 OpenSpec，可以通过两种方式与工作台配合使用。

**信息总结**：把 OpenSpec 当做 PRD 来理解和梳理，产出是文档/总结。

```
你：用工作台帮我梳理一下 OpenSpec 里 feature-login 的变更内容
AI：[创建 summary 场景工作区] → [阅读 proposal.md、tasks.md] → [产出结构化总结]
```

适合：想先理解变更内容、提取关键信息，再决定如何执行。

**直接导入**：把 OpenSpec 任务结构批量映射为工作区节点，可直接开始执行。

```
你：把 OpenSpec 的 feature-login 导入到工作台
AI：[列出变更详情] → [确认后执行导入] → [直接创建工作区和对应节点]
```

适合：已确定要执行 OpenSpec 中的任务，想跳过规划直接开始。

> ⚠️ 这是实验功能，适合已有 OpenSpec 规范的项目。普通用户无需关注。

---

## 问题诊断

### FAQ：常见问题

**Q1：工作区是什么？我必须用吗？**

工作区是一个任务的完整记录，包括需求、计划、执行过程、结论。你不是必须用——但当任务超过 3 步时，工作区能帮 AI 保持清晰的思路，也让你能追踪进度。

**Q2：我可以不用工作台直接让 AI 做吗？**

可以。只要不提及"工作区"或"工作台"，AI 会直接执行而不创建工作区。比如直接说"帮我改个 bug"，AI 就会像平常一样工作。适合简单的一次性任务。

**Q3：为什么 AI 会问我确认计划？能跳过吗？**

这是防止 AI 跑偏的关键检查点。你可以说"以后不用确认了"，但建议保留——看一眼计划只需要 10 秒，但修复跑偏的结果可能要 10 分钟。

**Q4：任务太大了，AI 做不完怎么办？**

说"这个太大了，帮我拆分"。AI 会把大任务分解成多个小任务，每个都有明确的验收标准。

**Q5：AI 做错了怎么办？**

说"这个不对，重新做"或"回滚"。如果是派发模式，代码会自动回滚；如果不是，你可能需要手动 git reset。

**Q6：怎么看 AI 做了什么？**

- **日常查看**：打开 WebUI，直观展示任务树、节点详情、执行日志
- **查看元数据**：只有需要查看底层管理数据时，才去 `.tanmi-workspace/` 目录

**Q7：关掉对话后工作区还在吗？**

在。工作区存储在项目目录的 `.tanmi-workspace/` 下，不会因为关闭对话而丢失。下次说"继续"就能恢复。

**Q8：一个项目可以有多个工作区吗？**

可以。每个工作区是独立的任务，你可以同时进行多个任务。但建议完成一个再开始下一个，避免上下文混乱。

**Q9：派发模式和普通模式有什么区别？**

详见 [进阶功能 > 派发模式](#派发模式适合复杂任务)。简单来说：普通模式由主 AI 直接执行，派发模式将子任务交给专门的 executor，适合可拆分的中大型任务。

**Q10：为什么有时候 AI 不创建工作区？**

因为你没有明确提及"工作区"或"工作台"。试试说"用工作台帮我做xxx"或"创建一个工作区"。

**Q11：我可以手动操作节点吗？**

| 操作类型 | 能否操作 | 说明 |
|----------|---------|------|
| 节点逻辑关系（添加/删除/状态转换） | WebUI 可操作 | 仅在 AI 出现重要纰漏时使用，AI 会在下次调用时收到通知 |
| 节点内容（需求/结论/笔记） | 需修改原始数据 | AI 总结不准确时可人工修正，但不会通知 AI |

**原则**：优先让 AI 自行修复问题。手动操作是最后手段。

**Q12：我手动改了代码，会影响工作区吗？**

不会。工作区只记录任务结构和执行日志，不跟踪代码文件变化。你可以随时手动修改代码，工作区不会受到影响。但建议在 AI 执行任务期间避免同时修改相关代码，以免产生冲突。

**Q13：WebUI 打不开怎么办？**

1. **检查服务状态**：`tanmi-workspace webui status`
2. **重启服务**：`tanmi-workspace webui restart`，然后刷新页面
3. **端口被占用**：`HTTP_PORT=19542 tanmi-workspace webui`
4. **工作区加载异常**：WebUI 设置页面有「诊断修复」功能，可自动检测并修复索引问题；或运行 `tanmi-workspace repair` 命令

**Q14：工作区完成后怎么处理？暂停任务怎么办？**

详见 [进阶功能 > 工作区管理](#工作区管理)。简单来说：完成后建议归档而非删除；暂停前让 AI 记录当前进度。

### 发现问题的信号

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| AI 直接执行不问确认 | 跳过了计划确认步骤 | 说"先展示计划让我确认" |
| 没有告知 WebUI 地址 | AI 遗漏关键步骤 | 说"WebUI 地址是什么" |
| 节点直接变成完成 | AI 跳步操作 | 说"请先 start 节点再执行" |
| AI 长时间没响应 | 执行卡住或出错 | 说"怎么了？有什么问题吗" |
| AI 说"我不确定" | 信息收集不充分 | 说"先收集信息再规划" |
| 结论很模糊 | AI 没有总结 | 说"这个节点的结论是什么" |
| 做了一半说完成了 | 验收标准不清晰 | 说"验收标准是什么？都满足了吗" |

### 纠正 AI 的标准话术

当你发现 AI 行为不对时，可以用这些话术纠正：

| 问题 | 你可以说 |
|------|----------|
| 跳过确认 | "先展示计划让我确认" |
| 没给链接 | "WebUI 地址是什么" |
| 跳步操作 | "请先 start 节点再执行" |
| 没记录 | "把这个记下来" |
| 忘记结论 | "这个节点的结论是什么" |
| 任务太大 | "这个太大了，拆分一下" |
| 做错了 | "这个不对，重新做" |
| 卡住了 | "有什么问题吗？需要我帮忙吗" |
| 想放弃 | "这个先不做了" |

---

## 参考速查

### 触发词速查

| 意图 | 说法 |
|------|------|
| 创建工作区 | "用**工作台**帮我做xxx"、"创建一个**工作区**" |
| 恢复任务 | "看看工作区状态"（推荐先查看再继续） |
| 查看进度 | 进度、做到哪了、状态、现在什么情况 |
| 确认计划 | 好、可以、继续、就这样、开始 |
| 调整计划 | 不对、调整一下、先不做、跳过 |
| 重新执行 | 重新做、再试一次、重来 |
| 放弃任务 | 先不做了、取消、算了 |

> **注意**：创建工作区必须明确提及"工作区"或"工作台"，否则 AI 会直接执行而不创建工作区。

### 数据存储位置

```
项目目录/
└── .tanmi-workspace/
    └── <工作区名称>_<id>/
        ├── graph.json      # 节点树结构
        ├── Info.md         # 工作区信息
        └── nodes/
            └── <节点名称>_<id>/
                ├── Info.md  # 节点信息
                └── Log.md   # 执行日志
```

---

## 已知局限

以下是当前版本的已知局限。我们区分了"可改进"（未来可能通过优化解决）和"架构性"（由设计决定，短期不会改变）两类。

### 可改进的局限

**长对话下 AI 对工作台的唤起能力下降**

当单个对话流程较长时，AI 可能逐渐"忘记"主动使用工作台工具，回到普通对话模式。这是 AI 上下文管理的固有挑战。

- **表现**：AI 开始直接回答而不更新节点，或忘记记录结论
- **应对**：提醒 AI"用工作台记录一下"或"更新节点状态"
- **展望**：随着 AI 能力提升和提示优化，此问题有望改善

**上下文恢复依赖记录完整性**

暂停任务后恢复时，AI 能恢复多少上下文取决于之前记录的完整性。如果节点的 conclusion/notes 记录不充分，恢复后 AI 可能遗漏关键信息。

- **表现**：恢复后 AI 对之前的决策细节不清楚
- **应对**：暂停前让 AI 充分记录当前状态；恢复后先查看节点内容确认
- **展望**：可通过更智能的自动记录机制改善

**需求变更后节点结构可能不够理想**

在项目进行中二次添加需求或修改需求时，新增的节点结构可能与原有规划不协调，或组织方式不够美观。这是由工作台的上下文机制和 AI 当时的上下文状态共同决定的。

- **表现**：新增节点位置不合理，或与原有节点的层级关系混乱
- **应对**：添加需求后，可让 AI "重新整理一下工作区结构"或具体描述期望的组织方式
- **展望**：可通过增强引导机制优化，但由于 AI 决策的动态性，难以彻底解决

### 架构性的局限

**派发模式需要 AI 支持 subagent**

派发模式依赖 AI 助手的 subagent（子代理）能力来执行子任务。并非所有 MCP 兼容的 AI 助手都完整支持此功能。

- **Claude Code**：完全支持
- **Cursor**：完全支持
- **OpenCode**：完全支持
- **其他 MCP 助手**：取决于具体实现

这是 MCP 协议生态的现状，非工作台本身的限制。

**历史工作区需手动管理**

工作区不会自动删除或归档。随着使用时间增长，`.tanmi-workspace/` 目录下会积累历史工作区。

- **设计原因**：自动清理可能误删有价值的历史记录
- **应对**：定期归档已完成的工作区；不再需要的可手动删除

---

*本手册适用于 TanmiWorkspace v1.x*

